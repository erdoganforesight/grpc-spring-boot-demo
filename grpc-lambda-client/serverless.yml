service: grpc-lambda-client
frameworkVersion: '2'

provider:
  name: aws
  runtime: java11
  environment:
    QUEUE_NAME: TriggerQueue
    SQS_URL:
      Ref: MyQueue
  iamRoleStatements:
    - Effect: Allow
      Action:
        - sqs:SendMessage
        - sqs:SendMessageBatch
        - sqs:ChangeMessageVisibility
        - sqs:DeleteMessage
        - sqs:GetQueueAttributes
        - sqs:ReceiveMessage
      Resource:
        Fn::GetAtt:
          - MyQueue
          - Arn

package:
  artifact: target/grpc-lambda-client.jar

functions:
  starter:
    handler: demo.Starter
    reservedConcurrency: 1
    timeout: 30
  sender:
    handler: demo.Sender
    reservedConcurrency: 100
    timeout: 30
    environment:
      GRPC_SERVER_ADDRESS: "dns:///service.erdoganyesil.link:9095"
      REQUEST_COUNT: "1000"
    events:
      - sqs:
          arn:
            Fn::GetAtt:
              - MyQueue
              - Arn
          batchSize: 1

resources:
  Resources:
    MyQueue:
      Type: AWS::SQS::Queue
      Properties:
        QueueName: ${self:provider.environment.QUEUE_NAME}
        VisibilityTimeout: 60
